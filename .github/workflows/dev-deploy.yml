name: Dev Deploy

on:
  push:
    branches:
      - dev

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up docker buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker image
        run: docker build -t ajmdrstalha/py_portfolio:dev .

      # Note: Running the container and checking it via curl on localhost will not work in GitHub Actions.
      # If you want to test the container, consider using integration tests or GitHub Actions services.

      # Optional: Push to Docker Hub or another registry if needed
      # - name: Log in to DockerHub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      #
      # - name: Push Docker image
      #   run: docker push ajmdrstalha/py_portfolio:dev

      - name: Deployment completed
        run: echo "Deployment completed successfully!"